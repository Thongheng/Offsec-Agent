{"schema": "offsec-leads/1", "fields": {"id": "short stable id, e.g. L-001", "ts": "ISO timestamp the lead was observed", "slice_id": "feature/area label", "target": "the exact element: endpoint/param/header/route", "observation": "what was seen that does not yet make sense, or the class named but not fired", "why": "why it could matter (impact if the anomaly is real)", "state": "open | worked | killed", "follow_up": "the specific next attempt that would resolve it", "resolution": "when worked/killed: what was found, or why it is dead (with evidence_ref)", "evidence_ref": "pointer to log.jsonl entry / pocs/<id>.md"}}
{"id": "L-001", "ts": "2026-01-01T12:05:00Z", "slice_id": "auth", "target": "GET /loginFromToken?token=@tok-<40hex>", "observation": "EXAMPLE ROW — delete. Login-via-token route returns 200; token format is guessable-shaped and expiry/reuse unverified.", "why": "If tokens are predictable or replayable past expiry, it is account takeover.", "state": "open", "follow_up": "Issue a token, attempt replay after use, attempt replay after expiry, probe for structure/entropy.", "resolution": null, "evidence_ref": null}
{"id": "L-002", "ts": "2026-01-01T12:10:00Z", "slice_id": "csrf", "target": "POST /api/rest/v1/settings (state-changing)", "observation": "EXAMPLE ROW — delete. CSRF is a payable class for this program and was named but never fired.", "why": "State-changing admin endpoint; if the token/session model permits cross-site submission, it is a config-takeover.", "state": "open", "follow_up": "Read the CSRF/token model, craft a cross-site form/fetch PoC against the settings write, check SameSite + split-token binding.", "resolution": null, "evidence_ref": null}
